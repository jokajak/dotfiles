-- dressing.nvim
-- They exist to allow plugin authors to override them with improvements upon
-- the default behavior, so that's exactly what we're going to do.

local M = {
  "stevearc/dressing.nvim",
  event = "UIEnter",
}

return M
