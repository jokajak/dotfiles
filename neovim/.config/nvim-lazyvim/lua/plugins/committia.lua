-- https://github.com/rhysd/committia.vim
-- This plugin improves the git commit buffer.

local M = {
  "rhysd/committia.vim",
  lazy = false, -- cannot be lazy loaded so that it's ready for git commit messages
}

return M
